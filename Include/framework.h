#pragma once

// This header must NEVER include Windows or DX headers directly.
// All platform headers are centralized in `Win32Common.h` and `DX12Common.h`.

#include "Win32Common.h"

// Standard C/C++ headers are fine
#include <cstdlib>
#include <cstring>
#include <memory>

