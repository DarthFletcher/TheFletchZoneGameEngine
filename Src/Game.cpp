#include "Game.h"

bool Game::Initialize() { // ✅ Ensure function returns `bool`
    // Initialize game state
    return true; // ✅ Explicit return statement
}

void Game::Update(float deltaTime) {
    // Update game logic
}

void Game::Shutdown() {
    // Cleanup resources
}
